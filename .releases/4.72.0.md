**New Features**

* Updated `SesProvider.sendEmail` to accept optional `attachments` and `headers` arguments.

**Technical Enhancements**

* Removed the internal Nodemailer transport from `SesProvider` and standardized email delivery on direct `SESV2.sendEmail` calls.
