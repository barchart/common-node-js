**Other**

* Performed significant refactoring of the `SqsProvider` class, attempting to root out the eventual failure of queue observers.
* Added a "watchdog" to monitor and restart queue observers in the `SqsProvider` class.